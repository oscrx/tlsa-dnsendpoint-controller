# Claims this OCI repository for the Artifact Hub listing, which is what earns
# the verified publisher badge. Not part of the release pipeline: it is a static
# file that only changes if the repository is re-registered, so it is pushed by
# hand with oras under the reserved artifacthub.io tag. An OCI registry has no
# path to serve a file from, which is why this is an artifact rather than a file
# sitting next to the chart.
#
#   oras push ghcr.io/oscrx/charts/tlsa-dnsendpoint-controller:artifacthub.io \
#     --config /dev/null:application/vnd.cncf.artifacthub.config.v1+yaml \
#     artifacthub-repo.yml:application/vnd.cncf.artifacthub.repository-metadata.layer.v1.yaml
repositoryID: 815b9b90-3e8d-4421-8923-24c3b9b16b04
